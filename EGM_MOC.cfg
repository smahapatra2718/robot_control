MOC:CFG_1.0:6:0::
#
# Default EGM tuning entry, referenced by EGMSetupUC's third argument. Name
# "default" matches the EGM_EXT_NAME constant in PyEgm.mod. Level "Global"
# means these settings apply to all EGM sessions unless overridden inline
# in RAPID via EGMActJoint flags (\LpFilter, \MaxSpeedDeviation, etc).
#
EXTERNAL_MOTION_INTERFACE_DATA:

      -Name "default" -Level "Global"

#
